============
Installation
============

At the command line::

    $ pip install python-mysdk

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-mysdk
    $ pip install python-mysdk